/**
 * Init- Git init is used to create a new repo
 * Create a new folder
 * go to that folder
 * Enter command in terminal >ls
 * >ls -h (which will tell wheather git is intialised or not)
 * >git init
 * >now check again >ls -h (now some .git file you will find but it will be hidden)
 * in terminal tou can see.
 * 
 * Go to git hub 
 * Create a new repo without README.me file
 * Then you can get the new repo https link copy that one
 * Come to VS code
 * >git remote add origin https......link (which build a connection between your local repo and remote repo
 * Nothing but git hub new repo)
 * 
 * Yo check the status of wheather your local repo is connected to remote repo
 * >git remote -v
 */

var number =10;